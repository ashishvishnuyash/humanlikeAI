# OpenAI
OPENAI_API_KEY=sk-...

# Pinecone
PINECONE_API_KEY=...
PINECONE_INDEX_NAME=uma-rag

# Firebase
FIREBASE_CREDENTIALS_PATH=firebaseadmn.json
FIREBASE_PROJECT_ID=mindtest-94298
FIREBASE_STORAGE_BUCKET=mindtest-94298.firebasestorage.app

# Email (Resend — https://resend.com)
RESEND_API_KEY=re_...
EMAIL_FROM=Diltak <noreply@diltak.com>

# Sessions
SESSION_TTL_DAYS=7

# Azure Postgres (migration target)
# Password special chars must be URL-encoded (@ -> %40)
DATABASE_URL=postgresql+psycopg2://diltak_db:Backend%40DB14@diltakdb.postgres.database.azure.com:5432/postgres?sslmode=require

# Auth (post-migration JWT)
# Generate JWT_SECRET with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=
JWT_ACCESS_MINUTES=15
JWT_REFRESH_DAYS=30

# Azure Blob Storage (provision in Phase 5, leave blank for now)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_ACCOUNT_NAME=
